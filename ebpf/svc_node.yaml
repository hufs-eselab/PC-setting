apiVersion: v1
kind: Service
metadata:
  name: mc-kube-controller
  namespace: mc-kube-system
spec:
  type: NodePort
  selector:
    control-plane: controller-manager
  ports:
    - name: overrun
      port: 8090
      targetPort: 8090
      nodePort: 30090
      protocol: TCP
