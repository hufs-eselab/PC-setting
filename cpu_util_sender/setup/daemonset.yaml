apiVersion: v1
kind: ServiceAccount
metadata:
  name: mckube-cpu-agent
  namespace: mc-kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: mckube-cpu-agent
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get", "list", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: mckube-cpu-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: mckube-cpu-agent
subjects:
- kind: ServiceAccount
  name: mckube-cpu-agent
  namespace: mc-kube-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mckube-cpu-agent
  namespace: mc-kube-system
  labels:
    app: mckube-cpu-agent
spec:
  selector:
    matchLabels:
      app: mckube-cpu-agent
  template:
    metadata:
      labels:
        app: mckube-cpu-agent
      annotations:
        container.apparmor.security.beta.kubernetes.io/cpu-agent: unconfined
    spec:
      serviceAccountName: mckube-cpu-agent
      hostNetwork: true
      hostPID: true
      tolerations:
      # Master node에도 배포되도록 toleration 추가
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      containers:
      - name: cpu-agent
        image: noru0817/cpu_util_sender:0.0.8
        imagePullPolicy: IfNotPresent
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        ports:
        - containerPort: 8888
          name: api-port
          protocol: TCP
          hostPort: 8888
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        securityContext:
          privileged: false
          runAsUser: 0
          readOnlyRootFilesystem: false
        livenessProbe:
          httpGet:
            path: /health
            port: 8888
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8888
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: proc
          mountPath: /proc
          readOnly: true
        - name: sys
          mountPath: /sys
          readOnly: true
      volumes:
      - name: proc
        hostPath:
          path: /proc
      - name: sys
        hostPath:
          path: /sys
      restartPolicy: Always